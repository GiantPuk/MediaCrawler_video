# 技术报告
## 目录

- [技术报告](#技术报告)
  - [目录](#目录)
  - [1. 项目定位](#1-项目定位)
    - [1.1 与上游项目的关系](#11-与上游项目的关系)
  - [2. 系统结构](#2-系统结构)
    - [2.1 分层结构](#21-分层结构)
    - [2.2 后端模块](#22-后端模块)
    - [2.3 前端模块](#23-前端模块)
  - [3. API 设计](#3-api-设计)
    - [3.1 配置接口](#31-配置接口)
    - [3.2 平台登录接口](#32-平台登录接口)
    - [3.3 任务接口](#33-任务接口)
  - [4. 任务模型](#4-任务模型)
    - [4.1 请求参数](#41-请求参数)
    - [4.2 工作流](#42-工作流)
    - [4.3 任务状态](#43-任务状态)
  - [5. 候选视频字段归一](#5-候选视频字段归一)
  - [6. 平台接入方式](#6-平台接入方式)
    - [6.1 B 站路径](#61-b-站路径)
    - [6.2 小红书路径](#62-小红书路径)
    - [6.3 抖音、快手、微博路径](#63-抖音快手微博路径)
  - [7. 下载与文件管理](#7-下载与文件管理)
    - [7.1 本地下载](#71-本地下载)
    - [7.2 断点续跑](#72-断点续跑)
    - [7.3 本地下载根目录](#73-本地下载根目录)
  - [8. OSS 转存链路](#8-oss-转存链路)
  - [9. 视频理解链路](#9-视频理解链路)
    - [9.1 云端模型配置](#91-云端模型配置)
    - [9.2 Whisper 文本融合](#92-whisper-文本融合)
    - [9.3 本地模型](#93-本地模型)
    - [9.4 提示词结构](#94-提示词结构)
  - [10. 结果文件](#10-结果文件)
  - [11. 前端交互](#11-前端交互)
  - [12. CLI 调用](#12-cli-调用)
  - [13. 运行记录](#13-运行记录)
  - [14. 单视频处理耗时参考](#14-单视频处理耗时参考)
  - [15. 部署与验证](#15-部署与验证)
  - [16. 使用说明](#16-使用说明)
## 1. 项目定位

MediaCrawler Video Workbench 基于 `NanmiCoder/MediaCrawler` 扩展，面向视频检索、候选筛选、下载、内容理解和结果管理等流程。项目保留原仓库的平台登录、搜索、详情抓取、创作者采集和 JSON 存储能力，并在其上增加统一的后端任务编排、WebUI 操作界面和 CLI 调用入口。



项目的基本工作方式是“先获取候选，再选择处理”：平台检索阶段只抓取元数据，前端展示候选视频后，由用户勾选需要下载或理解的视频。这样可以减少无关下载，也便于在多平台、多账号、多模型配置下复用同一套任务接口。

从工程实现上看，视频工作台没有另起一套独立爬虫，而是把“平台访问”和“视频任务”拆开：平台访问尽量沿用上游已有实现，视频任务层负责把不同平台拿到的数据整理成统一结构，再继续执行下载、转存、理解和结果保存。这样做的好处是平台逻辑有清晰来源，后续如果上游平台适配更新，也更容易合并到当前项目中。

核心能力包括：

| 能力 | 说明 |
| --- | --- |
| 视频候选检索 | 支持关键词、作者、排行榜三类入口。 |
| 作者候选解析 | 作者名存在重名时，先返回候选作者信息，再加载指定作者的视频。 |
| 候选筛选 | 支持日期范围、视频类型、最大抓取上限、筛选后数量。 |
| 本地下载 | 支持 `.part` 临时文件、进度记录、下载目录配置和目录打开；勾选分析的视频必须落本地。 |
| 视频理解 | 支持 DashScope/Qwen、OpenAI-compatible、Ollama 本地模型。 |
| 文本融合 | 可选 Whisper 转录；启用后每条选中视频都会执行本地音频提取和转录，转录文本与标题、描述、字幕一起传入模型。 |
| OSS 转存 | 支持将本地或源流视频临时转存到阿里云 OSS，再用签名 URL 传入模型；源流转存可与本地下载并行准备。 |
| 结果渲染 | 前端支持 Markdown、表格、Mermaid mindmap 和结构化 JSON 查看。 |
| CLI 调用 | WebUI 与 CLI 共用后端接口，便于批处理和命令行验证。 |

### 1.1 与上游项目的关系

上游 MediaCrawler 主要解决“如何登录平台、如何搜索、如何获取详情、如何把平台记录写到本地”这类问题。本项目新增的部分主要解决“这些平台记录如何变成可选择的视频候选、如何按条件筛选、如何下载、如何送入多模态模型、如何把处理进度和结果展示给用户”。

因此，平台侧仍优先走 `main.py`、`media_platform/` 和 `store/` 中已有的路径；只有在视频任务需要额外字段、下载进度、作者候选、排行榜或统一结果结构时，才在后端补充新的封装逻辑。这样的边界能避免前端直接理解每个平台的原始字段，也能让 CLI 和 WebUI 使用同一种任务模型。

## 2. 系统结构

### 2.1 分层结构

项目由四层组成：

| 层级 | 主要文件 | 作用 |
| --- | --- | --- |
| 原 MediaCrawler 爬虫层 | `main.py`, `media_platform/`, `store/`, `config/base_config.py` | 复用各平台登录、搜索、详情、创作者和 JSON 写盘逻辑。 |
| 视频任务后端 | `api/routers/video_summary.py`, `api/schemas/video_summary.py`, `api/services/video_summary_manager.py` | 负责任务编排、候选筛选、下载、OSS、Whisper、Qwen/Ollama 调用和状态持久化。 |
| WebUI 前端 | `webui/src/components/video/VideoWorkspace.tsx`, `webui/src/lib/api.ts` | 提供搜索栏、候选视频列表、排序筛选、下载与理解、排行榜、设置管理和结果渲染。 |
| CLI 工具 | `tools/video_summary_cli.py` | 支持用终端启动任务、查询状态、停止/续跑、配置平台 Cookie、配置模型和 OSS。 |

### 2.2 后端模块

后端主要围绕 `VideoSummaryManager` 工作。该类负责读取配置、创建任务、调用平台入口、保存状态、管理下载、调用模型和写入结果。

| 模块 | 说明 |
| --- | --- |
| `api/main.py` | FastAPI 应用入口，挂载 crawler、data、video-summary、websocket 等路由。 |
| `api/routers/video_summary.py` | 视频任务相关 HTTP 接口。 |
| `api/schemas/video_summary.py` | Pydantic 请求和响应模型。 |
| `api/services/video_summary_manager.py` | 视频工作台核心调度逻辑。 |
| `tools/video_summary_cli.py` | 终端调用入口，参数最终转换为后端任务请求。 |

`VideoSummaryManager` 的职责偏编排，不承担具体平台页面解析。它会根据任务参数选择搜索、作者或排行榜入口，拿到原始平台记录后再做字段归一和日期过滤。下载、转录、OSS 上传、模型调用等阶段都被记录为子任务，便于前端展示进度，也便于任务中断后恢复。

### 2.3 前端模块

前端主要由 `VideoWorkspace.tsx` 实现。界面组织方式接近视频网站：上方搜索，中部候选列表，下方结果区，右上角进入设置页。

| 模块 | 说明 |
| --- | --- |
| `webui/src/components/video/VideoWorkspace.tsx` | 视频工作台主界面。 |
| `webui/src/lib/api.ts` | 前后端接口类型和请求封装。 |
| `webui/src/components/video/VideoSummaryPanel.tsx` | 早期视频任务面板，部分类型定义仍可参考。 |

前端通过轮询任务状态刷新候选、进度、子任务耗时和结果，不另建独立日志面板；阶段日志统一进入底部系统控制台。

前端的核心交互是“搜索 - 勾选 - 处理 - 查看结果”。搜索区不直接触发下载，候选列表也不假定所有视频都需要处理；只有用户明确勾选后，后端才进入下载和模型调用阶段。这样可以把检索和处理分开，也使排行榜、作者页和关键词搜索都能复用同一套候选卡片。

## 3. API 设计

### 3.1 配置接口

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/api/video-summary/settings` | `GET` | 读取当前默认视频理解配置。 |
| `/api/video-summary/settings` | `POST` | 保存当前默认视频理解配置。 |
| `/api/video-summary/settings/profiles` | `GET` | 列出多套模型配置档案。 |
| `/api/video-summary/settings/profiles` | `POST` | 新增模型配置档案。 |
| `/api/video-summary/settings/profiles/{profile_id}` | `PUT` | 修改模型配置档案。 |
| `/api/video-summary/settings/profiles/{profile_id}` | `DELETE` | 删除模型配置档案。 |
| `/api/video-summary/settings/profiles/{profile_id}/activate` | `POST` | 将某个档案设为默认。 |

模型配置中的敏感字段不会直接明文出现在列表接口中。前端需要查看时，通过“加载明文”单独读取。

配置接口支持多档案管理。比如可以同时保存 DashScope、OpenAI-compatible 和 Ollama 本地模型配置，并指定其中一套作为默认档案。任务启动时会读取当前默认档案；如果用户临时修改设置，后续新任务会使用新的默认值，已运行任务仍保留启动时的配置快照。

### 3.2 平台登录接口

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/api/video-summary/platform-credentials` | `GET` | 列出平台登录配置。 |
| `/api/video-summary/platform-credentials` | `POST` | 新增平台登录配置。 |
| `/api/video-summary/platform-credentials/{profile_id}` | `PUT` | 修改平台登录配置。 |
| `/api/video-summary/platform-credentials/{profile_id}` | `DELETE` | 删除平台登录配置。 |
| `/api/video-summary/platform-credentials/{profile_id}/activate` | `POST` | 设为平台默认配置。 |
| `/api/video-summary/platform-credentials/{profile_id}/self-test` | `POST` | 用当前配置访问平台轻量入口。 |
| `/api/video-summary/platform-credentials/qrcode-login/start` | `POST` | 启动扫码登录流程。 |
| `/api/video-summary/platform-credentials/qrcode-login/{task_id}` | `GET` | 查询扫码登录状态。 |

Cookie 与扫码登录保存的信息只需要有一种可用。扫码成功后，本质上仍是保存浏览器会话中的登录信息，后续任务可直接读取。

平台登录信息按平台和档案分开保存。这样同一个平台可以保留多套账号信息，例如一个扫码登录档案和一个手动 Cookie 档案。自测接口只做轻量访问，用来判断当前配置是否能正常进入平台接口。

### 3.3 任务接口

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/api/video-summary/creators/resolve` | `POST` | 根据作者名、主页链接或 ID 返回候选作者。 |
| `/api/video-summary/tasks/start` | `POST` | 启动候选检索、排行榜、下载或内容理解任务。 |
| `/api/video-summary/tasks/{task_id}` | `GET` | 查询任务状态、候选列表、子任务进度和结果。 |
| `/api/video-summary/tasks/{task_id}/stop` | `POST` | 停止任务。 |
| `/api/video-summary/tasks/{task_id}/resume` | `POST` | 从已保存状态继续任务。 |
| `/api/video-summary/tasks/{task_id}/download-dir/open` | `POST` | 打开当前任务下载目录。 |

## 4. 任务模型

### 4.1 请求参数

`VideoSummaryTaskRequest` 是视频任务的核心请求模型。常用字段如下：

| 参数 | 含义 |
| --- | --- |
| `platform` | 平台，例如 `xhs`, `dy`, `ks`, `bili`, `wb`。 |
| `source_mode` | 来源模式：`search`, `creator`, `ranking`。 |
| `workflow_mode` | 工作流：`metadata_only`, `selected_items`, `full`。 |
| `search_keyword` | 关键词搜索使用的文本。 |
| `creator_id` | 作者 ID、主页链接或已选择的候选作者标识。 |
| `ranking_type` | 榜单类型。 |
| `ranking_limit` | 榜单读取数量。 |
| `start_date/end_date` | 发布时间筛选范围。 |
| `max_crawl_items` | 平台侧原始抓取上限。 |
| `max_videos` | 筛选后保留的候选视频数量。 |
| `selected_item_ids` | 用户勾选的视频 ID。 |
| `enable_whisper_transcription` | 是否启用 Whisper 转录。 |
| `video_input_mode` | 视频输入模式，当前推荐 `auto`。 |
| `video_upload_backend` | 上传或输入链路，例如 `auto`, `oss`, `dashscope`, `openai`, `frames`。 |

`max_crawl_items` 和 `max_videos` 分别控制“最多向平台拿多少原始结果”和“最终保留多少个候选”。后端在抓取过程中同步筛选，满足 `max_videos` 后会提前结束当前任务。

这两个参数分开后，搜索结果较稀疏时可以把 `max_crawl_items` 设大一些，把 `max_videos` 设为实际想看的数量。后端会一边抓取一边检查发布时间和视频类型，筛满后停止，避免必须等到原始上限全部跑完。

### 4.2 工作流

| 工作流 | 说明 |
| --- | --- |
| `metadata_only` | 只获取候选视频，不下载视频文件。 |
| `selected_items` | 读取已有候选，只处理用户勾选的视频。 |
| `full` | 获取候选并处理符合条件的视频，主要用于 CLI 或小规模任务。 |

WebUI 默认采用前两步组合：第一次任务使用 `metadata_only`，用户勾选后再使用 `selected_items`。

这种拆分也让失败边界更清楚。候选检索失败时不会产生下载文件；下载或模型调用失败时，候选列表仍保留在源任务中，可以调整配置后继续处理同一批候选。

### 4.3 任务状态

任务状态写入 `task_state.json`，前端和 CLI 都通过该文件恢复状态。主要状态如下：

| 状态 | 含义 |
| --- | --- |
| `pending` | 已创建但尚未执行。 |
| `running` | 正在执行。 |
| `completed` | 全部阶段结束。 |
| `failed` | 执行过程中出现错误。 |
| `stopped` | 用户主动停止。 |

子任务状态记录在 `subtasks` 中，包括阶段名称、状态、开始时间、结束时间、耗时、进度百分比、下载/上传字节数、速度和错误信息。

前端进度条读取的也是这部分结构化状态，而不是从控制台文本中解析。控制台负责保留阶段日志和错误上下文，进度条负责展示当前阶段、速度、百分比和耗时，两者数据来源一致但展示重点不同。

常见子任务包括：

| 子任务 | 说明 |
| --- | --- |
| `metadata` | 平台候选检索。 |
| `download` | 视频下载。 |
| `whisper` | 音频提取和 Whisper 转录。 |
| `oss_upload` | OSS 上传。 |
| `model_video` | 视频理解模型调用。 |
| `aggregate` | 多视频汇总。 |
| `save_result` | 写入结果文件。 |

## 5. 候选视频字段归一

平台返回字段不完全一致，后端会整理为统一候选结构。前端只展示真实取得的字段，未返回的字段显示为空或 `-`。

字段归一是视频工作台能够跨平台工作的关键。B 站、抖音、快手、小红书和微博对标题、作者、播放量、封面、时长的命名和返回位置都不同；如果前端直接读取平台原始字段，就会导致每个平台都需要单独写一套列表和筛选逻辑。统一候选结构把差异集中在后端，前端只关心标准字段是否存在。

| 统一字段 | 来源示例 | 说明 |
| --- | --- | --- |
| `id` | 平台内容 ID、BV 号、aweme_id、note_id | 任务内部唯一标识。 |
| `platform` | 请求平台 | 候选所属平台。 |
| `title` | 标题、描述前缀、正文摘要 | 候选标题。 |
| `author_name` | up 主、博主、发布者 | 作者显示名。 |
| `author_id` | UID、sec_user_id、user_id | 作者平台 ID。 |
| `cover_url` | 封面图、动态封面 | 前端候选卡片展示。 |
| `publish_time` | 发布时间、创建时间 | 用于日期筛选。 |
| `duration_seconds` | 视频时长字段或详情解析 | 前端显示时长。 |
| `view_count` | 播放量、观看量 | 平台提供时显示。 |
| `like_count` | 点赞量 | 平台提供时显示。 |
| `comment_count` | 评论量 | 平台提供时显示。 |
| `video_size_bytes` | 下载头、平台文件信息 | 平台或下载阶段取得时显示。 |
| `source_url` | 原视频页链接 | 用于打开平台页面和必要时重新解析。 |
| `raw_record_path` | 本地原始记录 | 便于追溯平台原始 JSON。 |

候选字段归一的目标是让 WebUI、CLI、下载器和模型调用共享同一种输入结构，而不是让每个平台单独维护一套页面逻辑。

原始平台记录不会被丢弃。任务目录中会保留 raw JSON，候选项中也会记录原始记录路径。这样既能让界面显示统一字段，也能在需要排查某个平台字段缺失时回到原始响应。

## 6. 平台接入方式

| 平台 | 检索方式 | 作者方式 | 榜单方式 | 下载方式 |
| --- | --- | --- | --- | --- |
| B 站 | 原项目爬虫、B 站 Web API、Playwright/CDP 登录态 | 支持作者名称候选、UID、空间链接 | 支持热门、分区、每周等榜单 | 通过真实 `playurl` 下载，下载失败时可重新解析链接。 |
| 小红书 | 复用原项目搜索与详情抓取 | 支持主页链接或 creator ID | 当前主要使用搜索和作者入口 | 详情页获取到视频文件后下载。 |
| 抖音 | 复用原项目登录态、签名和平台接口 | 支持主页链接或 `sec_user_id` | 支持热搜词/话题榜，榜单项可继续检索视频 | 有真实视频直链时下载。 |
| 快手 | 复用原项目登录态和平台接口 | 支持主页链接或 creator ID | 支持热榜类入口 | 有真实视频直链时下载。 |
| 微博 | 复用原项目搜索、热搜和视频字段解析 | 支持主页链接或 UID | 支持热搜词二次检索 | 有真实视频直链时下载。 |

平台接入遵循一个原则：能从平台真实接口或原项目爬虫拿到的数据才进入候选和结果。对于只能拿到热词的榜单，界面会把它作为热词入口处理，再用该热词检索候选视频；对于榜单项本身就是视频的情况，则直接进入候选列表。

### 6.1 B 站路径

B 站同时使用原项目爬虫和 Web API。候选搜索可直接读取较完整的结构化字段；作者模式可先解析候选作者，再请求指定 UID 的投稿列表。下载阶段通过视频详情解析真实播放地址，保存到本地。

B 站下载失败时，任务会重新根据视频 ID 解析一次播放地址，再继续下载，避免旧链接失效导致整个任务中断。

B 站之所以可以做得更快，是因为部分 Web API 会直接返回较完整的结构化字段。对于关键词搜索和作者投稿列表，后端可以在较少页面访问中拿到标题、作者、发布时间、统计数据等信息，再把视频 ID 交给下载阶段处理。

### 6.2 小红书路径

小红书搜索结果页返回字段较分散，通常需要进入详情才能确认视频文件、封面、发布时间等字段，因此候选阶段会比 B 站更慢。项目复用原 MediaCrawler 的搜索与详情逻辑，取得视频文件后再进入下载与内容理解流程。

这类平台的候选阶段更接近“逐条补全信息”：先拿到搜索结果，再根据需要进入详情页补视频字段。前端看到的候选数量可能增长较慢，但每条候选的来源仍来自平台实际页面或接口。

### 6.3 抖音、快手、微博路径

抖音、快手和微博主要复用原项目的平台登录态、签名逻辑和接口解析。榜单类入口中，部分平台返回的是热词或话题，不一定直接返回视频；这类结果会作为关键词继续检索候选视频。

下载阶段只使用真实返回的视频文件地址。若平台没有返回可下载文件，任务会记录原因，不用估计链接代替。

抖音、快手和微博的视频链接往往带有时效参数或访问条件。任务状态会保留下载失败原因，方便用户判断是登录信息、链接有效期、平台返回字段还是网络传输问题。

## 7. 下载与文件管理

### 7.1 本地下载

下载器会把视频先写入 `.part` 文件：

```text
video.mp4.part
```

下载完成后再改名为：

```text
video.mp4
```

这样可以区分“正在下载、下载失败、下载完成”三种状态。前端进度条显示已下载大小、总大小、速度、百分比和当前视频。

下载阶段不会因为候选列表中有多个视频就默认全部处理。WebUI 会把用户勾选的视频 ID 传给后端，后端只处理这批 ID；CLI 也可以通过参数控制处理数量和来源任务。

对于“下载并理解”任务，后端会先确保每条选中视频都有本地文件。源 URL 直给模型或 OSS URL 成功只影响模型输入方式，不会跳过本地保存。这样可以保证后续 Whisper、结果复查和目录打开都有稳定的本地文件依据。

### 7.2 断点续跑

任务状态、候选列表和子任务进度会写入 `task_state.json`。如果任务被停止、后端重启或下载失败，可以从已保存状态继续。对于已有 `.part` 文件的下载，后端会尽量复用已有字节；若平台支持 Range 请求，可以从已下载位置继续。

继续任务时，后端先读取已有状态，再判断哪些候选已经完成、哪些子任务还在中间状态。已经完成的视频结果不会重复写入，未完成的视频会从可继续的阶段恢复。

### 7.3 本地下载根目录

默认目录：

```text
data/video_tasks/<task_id>/raw/<platform>/videos/<video_id>/video.mp4
```

设置本地下载根目录后，视频文件保存到：

```text
<local_download_root>/<task_id>/raw/<platform>/videos/<video_id>/video.mp4
```

任务状态和结果索引仍在 `data/video_tasks/<task_id>/` 下，前端通过 `local_download_dir` 字段定位实际下载目录。

## 8. OSS 转存链路

OSS 用于把本地视频或平台源流临时放到公网可访问地址，再交给支持 URL 输入的云端模型。它是临时传输层，不替代本地下载。

常用配置：

| 配置项 | 说明 |
| --- | --- |
| `oss_access_key_id` | 阿里云 AccessKey ID。 |
| `oss_access_key_secret` | 阿里云 AccessKey Secret。 |
| `oss_bucket` | Bucket 名称。 |
| `oss_endpoint` | Endpoint，例如 `oss-cn-beijing.aliyuncs.com`。 |
| `oss_region` | 地域，例如 `cn-beijing`。 |
| `oss_prefix` | 对象前缀，例如 `mediacrawler/video-summary`。 |
| `oss_signed_url_expire_seconds` | 签名 URL 有效秒数。 |
| `oss_cleanup_after_analysis` | 任务结束后是否删除本次上传的临时对象。 |

对象路径包含平台、任务 ID、视频 ID 和短哈希，便于定位同一任务下的文件：

```text
<oss_prefix>/<platform>/<task_id>/<video_id>/<video_id>_<hash>.mp4
```

对于超过阈值的视频，上传使用 multipart 方式，前端可显示上传进度、上传速度和耗时。`auto` 或 `oss` 上传模式下，如果平台源流可读，后端会先启动“源流转 OSS”任务，同时继续执行本地下载和 Whisper；后续模型调用可以直接使用已经准备好的签名 URL。

开启任务结束后清理后，本次上传的临时对象会在模型调用结束后删除；本地任务目录仍保留下载文件和结果文件。

## 9. 视频理解链路

### 9.1 云端模型配置

云端模型支持 DashScope 官方接口和 OpenAI-compatible 接口。常用配置项如下：

| 配置项 | 说明 |
| --- | --- |
| `provider` | 接口来源，例如 `dashscope`, `openai_compatible`, `ollama`。 |
| `model` | 模型名称，例如 `qwen-vl-max`, `qwen3.5-omni-plus`，也可以手动输入服务商支持的模型名。 |
| `base_url` | 兼容接口地址。DashScope 默认使用 `https://dashscope.aliyuncs.com/compatible-mode/v1`。 |
| `api_key` | 模型服务密钥。 |
| `video_upload_backend` | 视频传入方式，例如 `auto`, `oss`, `dashscope`, `openai`, `frames`。 |

`auto` 模式会根据配置、视频大小和模型来源选择可用输入链路。对于云端模型，常见链路包括源 URL、OSS 签名 URL、本地文件上传、小视频 base64、抽帧图片。无论模型最终使用哪一种输入链路，选中分析的视频都会先保存到本地。

源 URL 是最快的输入方式，但它要求模型服务端能够直接访问该 URL。很多平台视频地址依赖 Cookie、Referer 或短期签名，模型服务端未必能打开；OSS 签名 URL 的作用就是把本地可访问或后端可读取的视频转换成模型服务端也能访问的临时地址。

### 9.2 Whisper 文本融合

Whisper 是可选步骤。启用后，后端会对每条选中视频先用 ffmpeg 提取音频，再调用基于 PyTorch 的 openai-whisper。Whisper 原生返回分段时间戳，项目会把这些时间戳和文本整理为字幕上下文，再与标题、描述、平台字幕等一同传入视频理解提示词。若未生成可用转录，该条视频会标记为失败，避免界面上显示“启用 Whisper”但实际没有转录参与分析。

日志和前端进度会记录使用的 Whisper 实现、设备、精度和模型名，例如：

```text
openai-whisper cuda/fp16=true, model turbo
```

### 9.3 本地模型

本地模型通过 Ollama 接入。由于不同本地视觉模型对视频输入的支持不一致，当前按照模型能力选择输入方式：

| 模型类型 | 处理方式 |
| --- | --- |
| 支持图片输入的本地 VLM | 使用 ffmpeg 抽取关键帧，将帧图像和文本上下文传给模型。 |
| 不支持视频或图片输入的文本模型 | 不作为视频理解模型使用。 |
| 云端 Omni/VL 模型 | 可以走本地文件上传、OSS URL 或服务商支持的多模态输入。 |

前端模型下拉框会标注模型来源，例如 DashScope、OpenAI-compatible 或 Ollama 本地。

### 9.4 提示词结构

单视频提示词主要包含：

| 部分 | 说明 |
| --- | --- |
| 任务说明 | 要求按时间顺序描述视频内容。 |
| 元数据 | 标题、作者、发布时间、平台、描述。 |
| 文本上下文 | 字幕、平台 AI 摘要、Whisper 转录等。 |
| 视频输入 | 视频 URL、本地上传文件、base64 小视频或抽帧图片。 |
| 输出格式 | 一句话概括、时间线摘要、主要内容、关键信息、标题一致性、标签。 |

多视频提示词主要包含单视频摘要集合，要求输出共同主题、各自内容梗概、合并摘要和 Mermaid mindmap。前端按 Markdown 渲染结果。

单视频提示词强调时间线，是为了让结果不仅给出主题，还能描述视频从开头到结尾发生了什么。多视频提示词则不再重复所有细节，而是把每个视频的摘要作为材料，整理共同主题和各自内容梗概。

## 10. 结果文件

任务数据保存在 `data/video_tasks/<task_id>/` 下。常见文件如下：

| 文件或目录 | 内容 |
| --- | --- |
| `task_state.json` | 任务状态、参数、阶段进度、错误信息、候选列表和结果索引。 |
| `result.json` | 单视频摘要、整体汇总、Markdown 文本、Mermaid 思维导图和模型调用信息。 |
| `raw/` | 平台爬虫原始 JSON 和下载文件。 |
| `raw/<platform>/videos/<video_id>/video.mp4` | 默认本地下载视频。 |
| `transcripts/` | Whisper 转录结果和时间戳。 |
| `logs/` | 任务日志。 |

`result.json` 中的关键字段：

| 字段 | 说明 |
| --- | --- |
| `task_id` | 任务 ID。 |
| `platform` | 平台。 |
| `source_mode` | 搜索、作者或排行榜。 |
| `items` | 分视频结果列表。 |
| `aggregate_summary` | 多视频合并摘要。 |
| `markdown` | 前端可直接渲染的 Markdown 文本。 |
| `mindmap` | Mermaid mindmap 文本。 |
| `local_download_dir` | 当前任务下载目录。 |
| `subtasks` | 子任务耗时和进度信息。 |

`result.json` 是前端结果渲染的主要数据源。Markdown 文本用于直接展示，结构化字段用于分视频卡片、状态标签、下载目录按钮和后续数据管理。这样即使 Markdown 渲染样式调整，原始结构化结果仍然可用。

## 11. 前端交互

前端界面包含：

| 区域 | 功能 |
| --- | --- |
| 顶部搜索栏 | 选择平台、搜索方式、关键词或作者、日期范围和数量。 |
| 候选视频区 | 展示封面、标题、作者、发布时间、播放量、点赞量、时长、视频大小等字段。 |
| 操作区 | 勾选候选视频后执行下载或下载并理解。 |
| 进度区 | 展示当前视频、下载/上传进度、速度、子任务耗时和模型名称。 |
| 结果区 | 展示分视频摘要、整体汇总、Markdown 和思维导图。 |
| 设置页 | 管理平台登录信息、模型配置、OSS、本地下载目录和抓取参数。 |
| 数据管理 | 按记录类型查看搜索、榜单、作者、内容、评论和视频理解结果。 |

设置页中“新增配置”区域直接展示在页面中，已保存配置可以查看明文、复制、修改、删除或设为默认。

候选列表支持较多字段，但不会强制要求每个平台都返回同样完整的信息。比如有的平台能返回播放量但没有文件大小，有的平台需要详情阶段才能知道时长。前端以统一卡片展示这些字段，缺失字段保持空值，避免把不同平台的数据混在一起解释。

## 12. CLI 调用

CLI 与 WebUI 使用同一套后端任务模型，常用命令如下：

CLI 的作用不是另做一套逻辑，而是把 WebUI 操作转换成可脚本化的命令。对于固定关键词、固定作者、定时检查或批量小样本测试，可以直接用 CLI 启动任务，再用任务 ID 查询结果。

| 命令 | 说明 |
| --- | --- |
| `settings get` | 查看当前模型配置。 |
| `settings set` | 保存模型配置。 |
| `credentials list` | 查看平台登录配置。 |
| `credentials self-test --platform bili` | 测试平台登录信息。 |
| `creators resolve --platform bili --query key725` | 解析候选作者。 |
| `tasks start` | 启动搜索、作者、排行榜或处理任务。 |
| `tasks get TASK_ID` | 查询任务状态。 |
| `tasks stop TASK_ID` | 停止任务。 |
| `tasks resume TASK_ID` | 继续任务。 |

关键词候选示例：

```shell
uv run python tools/video_summary_cli.py tasks start --platform bili --source-mode search --keyword "华南理工大学" --workflow-mode metadata_only --max-crawl-items 30 --max-videos 5
```

作者候选示例：

```shell
uv run python tools/video_summary_cli.py creators resolve --platform bili --query key725
uv run python tools/video_summary_cli.py tasks start --platform bili --source-mode creator --creator-id 123456 --workflow-mode metadata_only --max-crawl-items 30 --max-videos 5
```

## 13. 运行记录

本地任务目录中已有多轮平台检索、下载、转录、OSS 上传、Qwen/DashScope、Ollama 和结果渲染记录。任务状态分布示例：

| 状态 | 数量 |
| --- | ---: |
| `completed` | 73 |
| `error` | 13 |
| `running` | 2 |
| 合计 | 88 |

按平台和任务来源统计的样例：

| 平台 | 搜索任务 | 作者任务 | 榜单任务 |
| --- | ---: | ---: | ---: |
| B 站 | 23 | 18 | 12 |
| 抖音 | 4 | 2 | 2 |
| 快手 | 2 | 1 | 1 |
| 小红书 | 5 | 1 | 0 |
| 微博 | 3 | 1 | 1 |

子任务记录覆盖下载、Whisper 转录、OSS 上传、模型调用、聚合汇总、Mermaid 渲染等环节。前端进度条展示每个子任务的状态、耗时、下载速率、上传速率和错误原因；底部系统控制台保留阶段日志。

并发配置拆成两类：`crawl_concurrency` 只控制平台检索/爬取请求；`analysis_concurrency` 只控制已勾选视频的下载、Whisper、OSS 和模型分析。两者默认都为 1，便于保守测试；需要批量处理时，可以只调高分析并发，不增加平台爬取压力。

## 14. 单视频处理耗时参考

耗时与平台响应、登录状态、视频大小、网络带宽、是否启用 Whisper、是否走 OSS 以及模型响应速度有关。下面表格来自本地多轮任务日志和常见运行区间，便于估算一次“检索一个视频并处理”的等待时间。实际执行时，本地下载、源流 OSS 准备、Whisper 和模型分析是相对独立的阶段；在配置允许时，源流 OSS 准备可以和本地下载/Whisper 并行，因此整体耗时不一定等于各列简单相加。

| 场景 | 候选记录规模 | 视频/文件大小 | 候选检索 | 下载 | Whisper 转录 | OSS 上传 | 模型分析 | 整体参考 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| B 站 Web API 关键词，短视频 | 1-30 条 | 20-80 MB | 1-5 秒 | 10-60 秒 | 未启用时 0；GPU 常见 20-90 秒 | 关闭 OSS 时 0；开启时 10-90 秒 | 30-120 秒 | 1-4 分钟 |
| B 站 Playwright/CDP，短视频 | 1-30 条 | 20-80 MB | 10-30 秒 | 10-60 秒 | 未启用时 0；GPU 常见 20-90 秒 | 关闭 OSS 时 0；开启时 10-90 秒 | 30-120 秒 | 2-5 分钟 |
| B 站中长视频 | 1-10 条 | 100-500 MB | 10-30 秒 | 1-8 分钟 | GPU 常见 1-5 分钟 | 1-8 分钟 | 1-5 分钟 | 5-20 分钟 |
| 抖音/快手/微博短视频 | 1-20 条 | 3-80 MB | 10-45 秒 | 5-90 秒 | 未启用时 0；GPU 常见 10-90 秒 | 关闭 OSS 时 0；开启时 5-90 秒 | 30-120 秒 | 1.5-5 分钟 |
| 小红书短视频 | 1-20 条 | 5-60 MB | 20-90 秒 | 10-90 秒 | 未启用时 0；GPU 常见 10-90 秒 | 关闭 OSS 时 0；开启时 5-90 秒 | 30-120 秒 | 2-6 分钟 |
| 启用 Whisper GPU | 不变 | 任意 | 不变 | 不变 | 常见 20 秒-3 分钟 | 不变 | 可能略增，因为文本上下文更长 | 取决于视频时长 |
| 启用 Whisper CPU | 不变 | 任意 | 不变 | 不变 | 可能达到 GPU 的数倍 | 不变 | 可能略增，因为文本上下文更长 | 适合短视频 |
| OSS URL 传入云端模型 | 不变 | 2 MB 以上较常见 | 不变 | 仍保存本地文件 | 按配置决定 | 增加 5 秒-8 分钟，可与下载/转录重叠 | 常见 30 秒-5 分钟 | 适合长视频 |
| Ollama 本地视觉模型 | 不变 | 通常抽帧处理 | 不变 | 不变 | 可选 | 0 | 取决于本地模型和抽帧数 | 适合离线验证 |

模型分析阶段还需要结合具体模型和输入方式理解。下表只统计模型请求发出后到返回文本的时间，不包含候选检索、下载、Whisper 和 OSS 上传。

| 模型/链路 | 常见输入方式 | 输入规模 | 模型分析时间参考 |
| --- | --- | ---: | ---: |
| DashScope `qwen3.5-omni-plus` | OSS URL 或 DashScope 本地视频上传 | 短视频 3-80 MB | 30-150 秒 |
| DashScope `qwen3.5-omni-plus` | OSS URL | 中长视频 100-500 MB | 1-5 分钟 |
| DashScope `qwen-vl-max` | OSS URL、本地上传或抽帧 | 短视频 3-80 MB | 30-150 秒 |
| DashScope `qwen-vl-max` | 抽帧加文本上下文 | 4-16 帧 | 20-90 秒 |
| OpenAI-compatible 云端模型 | URL、base64 小视频或抽帧 | 取决于服务商限制 | 30-180 秒 |
| Ollama `qwen2.5vl:3b` 本地 | 抽帧加文本上下文 | 4-8 帧 | 20-120 秒 |
| Ollama 较大视觉模型 | 抽帧加文本上下文 | 4-8 帧 | 1-5 分钟 |

如果启用 Whisper，模型分析本身可能略有增加，因为提示词中会包含更长的转录文本；但主要新增耗时仍来自 Whisper 转录阶段。云端 Omni/VL 模型直接读视频时，模型分析时间通常比单纯抽帧更受视频时长影响。

小红书看起来比 B 站慢，主要原因是页面和详情字段更分散，往往需要逐条进入详情才能确认视频文件、封面和发布时间；B 站和快手在部分接口中能一次返回更多结构化字段，因此候选阶段更快。

## 15. 部署与验证

后端启动：

```shell
uv run python -m uvicorn api.main:app --host 127.0.0.1 --port 8080
```

前端启动：

```shell
cd webui
npm run dev -- --host 127.0.0.1 --port 5173
```

常用检查：

```shell
uv run python tools/video_summary_cli.py settings get
uv run python tools/video_summary_cli.py credentials list
uv run python tools/video_summary_cli.py credentials self-test --platform bili
uv run python tools/video_summary_cli.py tasks list
```

前端地址：

```text
http://127.0.0.1:5173/
```

后端地址：

```text
http://127.0.0.1:8080/docs
```

## 16. 使用说明

本项目仅用于个人学习、研究和技术验证。使用前需要确认目标平台规则、法律法规、账号安全、版权和数据合规要求。请勿用于商业采集、批量抓取、侵犯隐私、侵犯版权或其他违法违规行为。

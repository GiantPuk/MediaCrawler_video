# 技术报告

## 1. 项目定位

MediaCrawler Video Workbench 基于 `NanmiCoder/MediaCrawler` 扩展，面向视频检索、候选筛选、下载、内容理解和结果管理等流程。项目保留原仓库的平台登录、搜索、详情抓取、创作者采集和 JSON 存储能力，并在其上增加统一的后端任务编排、WebUI 操作界面和 CLI 调用入口。

当前流程强调“先获取候选，再选择处理”：平台检索阶段只抓取元数据，前端展示候选视频后，由用户勾选需要下载或理解的视频。这样可以减少无关下载，也便于在多平台、多账号、多模型配置下复用同一套任务接口。

## 2. 系统结构

项目由四层组成：

| 层级 | 主要文件 | 作用 |
| --- | --- | --- |
| 原 MediaCrawler 爬虫层 | `main.py`, `media_platform/`, `store/`, `config/base_config.py` | 复用各平台登录、搜索、详情、创作者和 JSON 写盘逻辑。 |
| 视频任务后端 | `api/routers/video_summary.py`, `api/schemas/video_summary.py`, `api/services/video_summary_manager.py` | 负责任务编排、候选筛选、下载、OSS、Whisper、Qwen/Ollama 调用和状态持久化。 |
| WebUI 前端 | `webui/src/components/video/VideoWorkspace.tsx`, `webui/src/lib/api.ts` | 提供搜索栏、候选视频列表、排序筛选、下载与理解、排行榜、设置管理和结果渲染。 |
| CLI 工具 | `tools/video_summary_cli.py` | 支持用终端启动任务、查询状态、停止/续跑、配置平台 Cookie、配置模型和 OSS。 |

后端使用 FastAPI 暴露接口，前端和 CLI 共用同一套 API。关键接口包括：

| 接口 | 用途 |
| --- | --- |
| `GET/POST /api/video-summary/settings` | 获取或保存默认视频理解 API 配置。 |
| `GET/POST/PUT/DELETE /api/video-summary/settings/profiles` | 管理多套模型配置档案。 |
| `GET/POST/PUT/DELETE /api/video-summary/platform-credentials` | 管理各平台 Cookie 或扫码登录保存的信息。 |
| `POST /api/video-summary/platform-credentials/{id}/self-test` | 测试当前平台登录信息是否可用。 |
| `POST /api/video-summary/creators/resolve` | 根据作者名称、主页链接或 ID 解析候选作者。 |
| `POST /api/video-summary/tasks/start` | 启动搜索、排行榜、下载或视频理解任务。 |
| `GET /api/video-summary/tasks/{task_id}` | 查询任务状态、子任务进度、候选列表和结果。 |
| `POST /api/video-summary/tasks/{task_id}/stop` | 停止任务。 |
| `POST /api/video-summary/tasks/{task_id}/resume` | 从已保存状态继续任务。 |

## 3. 任务流程

### 3.1 候选检索

候选检索支持三类来源：

| 来源模式 | 说明 |
| --- | --- |
| `search` | 将关键词提交给平台搜索入口，返回候选视频。 |
| `creator` | 先解析创作者，再加载该创作者发布的视频。 |
| `ranking` | 读取平台榜单或热榜条目，榜单视频可直接进入候选列表；热搜词类榜单可继续作为关键词检索视频。 |

检索阶段的核心参数：

| 参数 | 含义 |
| --- | --- |
| `platform` | 平台：`xhs`, `dy`, `ks`, `bili`, `wb` 等。 |
| `max_crawl_items` | 平台侧最大抓取上限，用于控制最多向平台请求多少条原始结果。 |
| `max_videos` | 经过日期、类型等条件筛选后保留的候选视频数量。 |
| `start_date/end_date` | 发布时间筛选范围。 |
| `creator_id` | 创作者 ID、主页链接或已选择的候选作者标识。 |

后端会在抓取过程中尽早筛选。如果已经得到满足条件的 `max_videos` 条视频，会提前结束本轮检索，不必等到 `max_crawl_items` 全部完成。

### 3.2 下载与理解

用户勾选候选视频后，任务进入下载与内容理解阶段：

1. 读取候选视频元数据。
2. 对选中视频解析可用视频链接。
3. 下载到本地目录，写入 `.part` 临时文件，完成后改名为 `video.mp4`。
4. 根据配置决定是否提取音频并调用 Whisper。
5. 将标题、描述、字幕、Whisper 转录等文本上下文传入视频理解 prompt。
6. 按配置使用 DashScope/Qwen、OpenAI-compatible 接口或 Ollama 本地模型。
7. 写入单视频摘要、整体汇总、Markdown 文本、Mermaid 思维导图和任务状态。

如果在设置中配置了本地下载根目录，视频文件会保存在该目录下；任务状态和结果索引仍保存在项目的 `data/video_tasks/` 中，方便 WebUI 和 CLI 查找。

## 4. 平台接入方式

| 平台 | 检索方式 | 作者方式 | 榜单方式 | 下载方式 |
| --- | --- | --- | --- | --- |
| B 站 | 原项目爬虫、B 站 Web API、Playwright/CDP 登录态 | 支持作者名称候选、UID、空间链接 | 支持热门、分区、每周等榜单 | 通过真实 `playurl` 下载，下载失败时可重新解析链接。 |
| 小红书 | 复用原项目搜索与详情抓取 | 支持主页链接或 creator ID | 当前主要使用搜索和作者入口 | 详情页获取到视频文件后下载。 |
| 抖音 | 复用原项目登录态、签名和平台接口 | 支持主页链接或 `sec_user_id` | 支持热搜词/话题榜，榜单项可继续检索视频 | 有真实视频直链时下载。 |
| 快手 | 复用原项目登录态和平台接口 | 支持主页链接或 creator ID | 支持热榜类入口 | 有真实视频直链时下载。 |
| 微博 | 复用原项目搜索、热搜和视频字段解析 | 支持主页链接或 UID | 支持热搜词二次检索 | 有真实视频直链时下载。 |

不同平台返回字段不完全一致，后端会统一整理为候选视频字段，例如标题、作者、封面、发布时间、播放量、点赞量、评论量、时长、视频大小和平台原始 ID。若平台响应本身没有提供某字段，前端会显示为空或 `-`，不会用估计值代替。

## 5. 视频理解链路

### 5.1 云端模型

云端模型支持 DashScope 官方接口和 OpenAI-compatible 接口。常用配置项如下：

| 配置项 | 说明 |
| --- | --- |
| `provider` | 接口来源，例如 `dashscope`, `openai_compatible`, `ollama`。 |
| `model` | 模型名称，例如 `qwen-vl-max`, `qwen3.5-omni-plus`，也可以手动输入服务商支持的模型名。 |
| `base_url` | 兼容接口地址。DashScope 默认使用 `https://dashscope.aliyuncs.com/compatible-mode/v1`。 |
| `api_key` | 模型服务密钥。 |
| `video_upload_backend` | 视频传入方式，例如 `auto`, `oss`, `dashscope`, `openai`, `frames`。 |

启用 OSS 后，长视频会先上传到临时 OSS 对象，再用带签名的公网 URL 交给模型。任务结束后可以自动删除 OSS 临时对象，减少 Bucket 中的残留文件。

### 5.2 Whisper 文本融合

Whisper 是可选步骤。启用后，后端会先用 ffmpeg 提取音频，再调用基于 PyTorch 的 openai-whisper。Whisper 原生返回分段时间戳，项目会把这些时间戳和文本整理为字幕上下文，再与标题、描述、平台字幕等一同传入视频理解 prompt。

日志和前端进度会记录使用的 Whisper 实现、设备、精度和模型名，例如：

```text
openai-whisper cuda/fp16=true, model turbo
```

### 5.3 本地模型

本地模型通过 Ollama 接入。由于不同本地视觉模型对视频的支持不一致，当前按照模型能力选择输入方式：

| 模型类型 | 处理方式 |
| --- | --- |
| 支持图片输入的本地 VLM | 使用 ffmpeg 抽取关键帧，将帧图像和文本上下文传给模型。 |
| 不支持视频或图片输入的文本模型 | 不作为视频理解模型使用。 |
| 云端 Omni/VL 模型 | 可以走本地文件上传、OSS URL 或服务商支持的多模态输入。 |

前端模型下拉框会标注模型来源，例如 DashScope、OpenAI-compatible 或 Ollama 本地。

## 6. 数据目录

任务数据保存在 `data/video_tasks/<task_id>/` 下。常见文件如下：

| 文件或目录 | 内容 |
| --- | --- |
| `task_state.json` | 任务状态、参数、阶段进度、错误信息、候选列表和结果索引。 |
| `result.json` | 单视频摘要、整体汇总、Markdown 文本、Mermaid 思维导图和模型调用信息。 |
| `raw/` | 平台爬虫原始 JSON 和下载文件。 |
| `raw/<platform>/videos/<video_id>/video.mp4` | 默认本地下载视频。 |
| `transcripts/` | Whisper 转录结果和时间戳。 |
| `logs/` | 任务日志。 |

如果设置了本地下载根目录，视频文件会放到：

```text
<local_download_root>/<task_id>/raw/<platform>/videos/<video_id>/video.mp4
```

WebUI 的任务结果区域提供“打开下载目录”按钮，用于定位当前任务的视频保存位置。

## 7. 运行记录

本地任务目录中已有多轮平台检索、下载、转录、OSS 上传、Qwen/DashScope、Ollama 和结果渲染记录。任务状态分布示例：

| 状态 | 数量 |
| --- | ---: |
| `completed` | 73 |
| `error` | 13 |
| `running` | 2 |
| 合计 | 88 |

按平台和任务来源统计的样例：

| 平台 | 搜索任务 | 作者任务 | 榜单任务 |
| --- | ---: | ---: | ---: |
| B 站 | 23 | 18 | 12 |
| 抖音 | 4 | 2 | 2 |
| 快手 | 2 | 1 | 1 |
| 小红书 | 5 | 1 | 0 |
| 微博 | 3 | 1 | 1 |

子任务记录覆盖下载、Whisper 转录、OSS 上传、模型调用、聚合汇总、Mermaid 渲染等环节。前端进度条展示每个子任务的状态、耗时、下载速率、上传速率和错误原因；底部系统控制台保留阶段日志。

## 8. 单视频处理耗时参考

耗时与平台响应、登录状态、视频大小、网络带宽、是否启用 Whisper、是否走 OSS 以及模型响应速度有关。下面表格来自本地多轮任务日志和常见运行区间，便于估算一次“检索一个视频并处理”的等待时间。

| 场景 | 视频/文件大小 | 候选检索 | 下载 | 转录/上传/模型 | 整体参考 |
| --- | ---: | ---: | ---: | ---: | ---: |
| B 站 Web API 关键词，短视频 | 20-80 MB | 1-5 秒 | 10-60 秒 | 30-120 秒 | 1-3 分钟 |
| B 站 Playwright/CDP，短视频 | 20-80 MB | 10-30 秒 | 10-60 秒 | 30-120 秒 | 2-4 分钟 |
| B 站中长视频 | 100-500 MB | 10-30 秒 | 1-8 分钟 | 1-5 分钟 | 4-15 分钟 |
| 抖音/快手/微博短视频 | 3-80 MB | 10-45 秒 | 5-90 秒 | 30-120 秒 | 1.5-5 分钟 |
| 小红书短视频 | 5-60 MB | 20-90 秒 | 10-90 秒 | 30-120 秒 | 2-6 分钟 |
| 启用 Whisper GPU | 任意 | 不变 | 不变 | 常见 20 秒-3 分钟 | 取决于视频时长 |
| 启用 Whisper CPU | 任意 | 不变 | 不变 | 可能达到 GPU 的数倍 | 适合短视频 |
| OSS URL 传入云端模型 | 2 MB 以上较常见 | 不变 | 需本地下载或边下边传 | 增加上传时间，模型可读公网 URL | 适合长视频 |
| Ollama 本地视觉模型 | 通常抽帧处理 | 不变 | 不变 | 取决于本地模型和抽帧数 | 适合离线验证 |

小红书看起来比 B 站慢，主要原因是页面和详情字段更分散，往往需要逐条进入详情才能确认视频文件、封面和发布时间；B 站和快手在部分接口中能一次返回更多结构化字段，因此候选阶段更快。

## 9. 结果格式

单视频结果包含：

| 字段 | 说明 |
| --- | --- |
| 一句话概括 | 对视频核心内容的简短概括。 |
| 时间线摘要 | 按视频时间顺序描述每一段内容。 |
| 主要内容 | 视频主题、结构和观点。 |
| 关键信息 | 人物、场景、物品、数字、事件等。 |
| 与标题一致性 | 判断标题与视频内容是否对应。 |
| 标签 | 便于后续检索的标签。 |

多视频结果包含：

| 字段 | 说明 |
| --- | --- |
| 共同主题 | 多个视频之间的共同内容方向。 |
| 各自内容梗概 | 每个视频的独立梗概。 |
| 摘要 | 对本次任务材料的合并说明。 |
| Mermaid 思维导图 | 可在前端渲染为结构图。 |

结果文件保存在 `result.json`，前端会以 Markdown 方式渲染标题、列表、表格和 Mermaid mindmap。

## 10. 部署与验证

后端启动：

```shell
uv run python -m uvicorn api.main:app --host 127.0.0.1 --port 8080
```

前端启动：

```shell
cd webui
npm run dev -- --host 127.0.0.1 --port 5173
```

常用检查：

```shell
uv run python tools/video_summary_cli.py settings get
uv run python tools/video_summary_cli.py credentials list
uv run python tools/video_summary_cli.py credentials self-test --platform bili
uv run python tools/video_summary_cli.py tasks list
```

前端地址：

```text
http://127.0.0.1:5173/
```

后端地址：

```text
http://127.0.0.1:8080/docs
```

## 11. 使用说明

本项目仅用于个人学习、研究和技术验证。使用前需要确认目标平台规则、法律法规、账号安全、版权和数据合规要求。请勿用于商业采集、批量抓取、侵犯隐私、侵犯版权或其他违法违规行为。

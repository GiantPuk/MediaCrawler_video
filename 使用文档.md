# 使用文档

本文说明 MediaCrawler Video Workbench 的日常使用流程，包括启动服务、配置账号、搜索候选视频、下载与内容理解、查看结果、设置本地下载目录，以及通过 CLI 调用同一套能力。

## 目录

- [1. 环境准备](#1-环境准备)
- [2. 启动前后端](#2-启动前后端)
- [3. 设置](#3-设置)
- [4. 搜索视频](#4-搜索视频)
- [5. 作者搜索](#5-作者搜索)
- [6. 排行榜](#6-排行榜)
- [7. 下载与内容理解](#7-下载与内容理解)
- [8. 查看结果](#8-查看结果)
- [9. 停止与继续](#9-停止与继续)
- [10. CLI 使用](#10-cli-使用)
- [11. 常见情况](#11-常见情况)
- [12. 文件索引](#12-文件索引)

## 1. 环境准备

需要安装：

| 工具 | 用途 |
| --- | --- |
| Python 3.11 | 后端、爬虫和任务调度。 |
| uv | Python 依赖管理和命令运行。 |
| Node.js 16+ | WebUI 前端。 |
| Chrome 或 Playwright 浏览器 | 平台登录、扫码和页面访问。 |
| ffmpeg | Whisper 转录和本地抽帧需要。 |
| CUDA 版 PyTorch | 可选，用于 Whisper GPU 转录。 |
| Ollama | 可选，用于本地视觉模型。 |

安装依赖：

```shell
uv sync

cd webui
npm install
cd ..
```

如果使用 Playwright 浏览器：

```shell
uv run playwright install
```

## 2. 启动前后端

后端固定使用 8080：

```shell
uv run python -m uvicorn api.main:app --host 127.0.0.1 --port 8080
```

前端固定使用 5173：

```shell
cd webui
npm run dev -- --host 127.0.0.1 --port 5173
```

打开 WebUI：

```text
http://127.0.0.1:5173/
```

查看后端接口：

```text
http://127.0.0.1:8080/docs
```

## 3. 设置

右上角进入设置页。设置页参考 Chrome 设置页组织方式，主要分为平台登录、视频理解 API、本地下载、基础抓取参数等部分。

### 3.1 平台登录信息

支持为不同平台保存 Cookie 或扫码登录后的信息。Cookie 与扫码登录保存的信息只需要有一种可用，任务即可使用对应平台登录态。

常用字段：

| 字段 | 说明 |
| --- | --- |
| 平台 | 选择 `bili`, `xhs`, `dy`, `ks`, `wb` 等平台。 |
| Cookie | 从浏览器复制的完整 Cookie。 |
| Headers | 部分平台可附加 User-Agent、Referer 等请求头。 |
| 扫码登录 | 打开平台登录二维码，扫码成功后保存浏览器会话中的 Cookie。 |
| 自测 | 用当前保存的信息访问平台轻量接口，确认是否可用。 |

浏览器复制 Cookie 的方式：

1. 打开对应平台网页并确认已登录。
2. 按 `F12` 打开开发者工具。
3. 进入 `Application` 或 `应用`。
4. 找到 `Cookies` 下的平台域名。
5. 复制关键 Cookie，或在 Network 请求中复制完整 `Cookie` 请求头。

不同平台的登录字段不同。若平台自测失败，应优先检查登录态字段是否完整、Cookie 是否过期、域名是否对应。

### 3.2 视频理解 API

可以配置多套模型档案。任务运行时使用当前默认档案。

| 字段 | 说明 |
| --- | --- |
| 档案名 | 便于区分多套配置，例如 `dashscope-omni`, `qwen-vl`, `ollama-local`。 |
| 接口类型 | DashScope 官方云端、OpenAI-compatible、Ollama 本地等。 |
| API Key | 云端模型密钥。本地 Ollama 通常不需要。 |
| 模型 | 可以从下拉候选选择，也可以直接输入服务商支持的模型名。 |
| Base URL | 兼容接口地址。 |
| OSS 转存上传 | 长视频可临时转存到 OSS，再用签名 URL 交给模型；勾选分析的视频仍会保存到本地。 |
| Whisper | 可选；启用后，勾选分析的每条视频都会先保存到本地并执行转录，再把转录文本加入视频理解上下文。 |

模型下拉框会标注来源，例如 DashScope、OpenAI-compatible 或 Ollama 本地，避免混淆。

### 3.3 本地下载目录

默认情况下，视频文件保存在：

```text
data/video_tasks/<task_id>/raw/<platform>/videos/<video_id>/video.mp4
```

如果希望保存在其他磁盘，可以在设置中填写本地下载根目录，例如：

```text
E:\MediaCrawlerDownloads
```

配置后，新任务的视频会保存到：

```text
E:\MediaCrawlerDownloads\<task_id>\raw\<platform>\videos\<video_id>\video.mp4
```

任务结果区会显示“打开下载目录”按钮，点击后可直接打开当前任务的视频目录。任务状态、候选列表和结果文件仍保存在项目的 `data/video_tasks/` 中。

### 3.4 基础抓取参数

| 参数 | 建议 |
| --- | --- |
| 最大抓取上限 | 平台侧原始抓取上限，用于控制最多向平台请求多少条。 |
| 筛选后数量 | 经过日期和视频类型筛选后保留多少条候选。 |
| 最小间隔 / 最大间隔 | 每条请求之间随机等待的区间。 |
| 每 N 条长暂停 | 每抓取若干条后进行一次较长等待。 |
| 抓取并发 | 默认 1，只控制平台检索/爬取请求。 |
| 分析并发 | 默认 1，只控制已勾选视频的下载、转录、OSS 和模型分析并行度。 |
| 评论抓取 | 视频理解任务默认关闭。 |
| 媒体下载 | 候选检索阶段默认关闭，勾选视频后再下载。 |

## 4. 搜索视频

主界面顶部是搜索区，使用方式类似视频网站：

1. 选择平台。
2. 选择搜索方式：关键词、作者、排行榜。
3. 输入关键词、作者名、主页链接或 ID。
4. 设置日期范围、最大抓取上限和筛选后数量。
5. 点击搜索。

搜索完成后，下方会显示候选视频列表。候选项通常包括：

| 字段 | 说明 |
| --- | --- |
| 封面 | 平台返回封面时显示。 |
| 标题 | 视频标题。 |
| 作者 | 发布者名称或 ID。 |
| 发布时间 | 平台返回的发布时间。 |
| 播放量、点赞量、评论量 | 平台返回对应字段时显示。 |
| 时长 | 平台返回或详情解析得到的视频时长。 |
| 视频大小 | 已解析到文件信息时显示。 |

若某个平台没有返回某字段，界面会显示 `-`，表示该字段缺失。

## 5. 作者搜索

选择“作者”后，可以输入作者名称、主页链接或平台 ID。

推荐流程：

1. 输入作者名称。
2. 后端返回候选作者卡片。
3. 根据头像、昵称、ID、粉丝数、视频数等信息选择具体作者。
4. 点击作者卡片后，再加载该作者符合时间条件的视频。
5. 勾选需要下载或理解的视频。

如果作者名称存在重名，应优先通过候选作者卡片确认 UID、主页链接和粉丝数，再进入视频列表。

## 6. 排行榜

选择“排行榜”后，可以读取平台提供的榜单或热榜入口。榜单分两类：

| 类型 | 说明 |
| --- | --- |
| 视频榜单 | 榜单项本身就是视频，可直接进入候选列表。 |
| 热词/话题榜 | 榜单项是关键词或话题，需要继续作为关键词检索视频。 |

不同平台开放的榜单类型不同，WebUI 只展示当前已接入且可真实调用的入口。

## 7. 下载与内容理解

候选列表出现后，可以勾选视频并选择：

| 操作 | 说明 |
| --- | --- |
| 下载 | 只下载视频到本地目录。 |
| 下载并理解 | 将视频保存到本地后调用模型，得到单视频时间线摘要和多视频汇总；如果启用 Whisper，每条选中视频都会执行转录。 |

任务运行时，前端会显示：

| 进度项 | 说明 |
| --- | --- |
| 当前视频 | 正在处理的视频标题或 ID。 |
| 下载进度 | 已下载大小、总大小、速度、百分比。 |
| OSS 进度 | 已上传大小、总大小、速度、百分比。 |
| 子任务耗时 | 下载、转录、上传、模型调用等阶段的执行时间。 |
| 模型信息 | 当前使用的模型、接口类型、Whisper 模型和设备。 |

系统控制台只保留统一日志，不再另开视频任务日志面板。

启用 OSS 时，源流转存 OSS 可以和本地下载、Whisper 并行准备；OSS 只是模型输入通道，不替代本地视频文件。若开启“分析结束后删除 OSS 临时视频”，模型调用结束后会清理本次上传对象。

## 8. 查看结果

任务完成后，结果区显示：

| 结果 | 内容 |
| --- | --- |
| 分视频结果 | 每个视频的一句话概括、时间线摘要、主要内容、关键信息和标签。 |
| 整体汇总 | 多个视频的共同主题、各自内容梗概和合并摘要。 |
| 思维导图 | Mermaid mindmap，可在前端渲染为结构图。 |
| 原始 JSON | 可查看 `result.json` 中的结构化结果。 |

结果文件位置：

```text
data/video_tasks/<task_id>/result.json
data/video_tasks/<task_id>/result.md
```

WebUI 结果区提供“复制 Markdown”和“下载 Markdown”。`result.md` 是便于直接阅读和归档的文本结果，`result.json` 是结构化结果，适合后续程序处理。

Whisper 转录文件位置：

```text
data/video_tasks/<task_id>/transcripts/
```

## 9. 停止与继续

任务运行时可以点击停止。停止后，后端会保存当前状态。

继续任务时会读取：

| 文件 | 作用 |
| --- | --- |
| `task_state.json` | 记录任务状态、候选、已完成视频、失败阶段和进度。 |
| `.part` 文件 | 下载中的临时文件。 |
| `result.json` | 已完成的视频结果。 |

若下载链接已过期，B 站任务会尝试重新解析真实视频链接后继续下载。

## 10. CLI 使用

终端支持视频任务的批量检索、批量下载、批量分析、停止、续跑、登录信息管理和模型配置管理。WebUI 底部的“系统控制台”只显示日志，不是可输入命令的 shell；以下命令需要在 PowerShell、CMD 或其他终端中运行。CLI 没有单独命名为 `batch` 的子命令，批量能力通过 `tasks start` 的 `max-videos`、`workflow-mode` 和 `selected-items` 参数实现。

CLI 调用本地后端 API，因此需要先启动后端：

```shell
uv run python -m uvicorn api.main:app --host 127.0.0.1 --port 8080
```

CLI 入口：

```text
tools/video_summary_cli.py
```

查看帮助：

```shell
uv run python tools/video_summary_cli.py --help
uv run python tools/video_summary_cli.py tasks start --help
```

### 10.1 平台登录信息

查看已保存的平台 Cookie/扫码登录档案：

```shell
uv run python tools/video_summary_cli.py credentials list
```

新增 Cookie 档案：

```shell
uv run python tools/video_summary_cli.py credentials create --platform bili --name "bili-main" --cookies-file .\bili.cookie.txt
```

设为默认登录档案：

```shell
uv run python tools/video_summary_cli.py credentials activate <credential_profile_id>
```

检查登录档案健康状态：

```shell
uv run python tools/video_summary_cli.py credentials health <credential_profile_id>
```

扫码登录并保存 Cookie：

```shell
uv run python tools/video_summary_cli.py credentials qrcode-login --platform bili --name "bili-qrcode"
```

扫码登录任务如果没有等待完成，可以手动查询：

```shell
uv run python tools/video_summary_cli.py credentials qrcode-status <login_task_id>
uv run python tools/video_summary_cli.py credentials qrcode-wait <login_task_id>
```

### 10.2 视频理解 API 配置

查看模型档案：

```shell
uv run python tools/video_summary_cli.py qwen list
```

新增 DashScope 档案：

```shell
uv run python tools/video_summary_cli.py qwen create --name "dashscope-main" --api-provider dashscope --base-url https://dashscope.aliyuncs.com/compatible-mode/v1 --model qwen3.5-omni-plus --api-key-file .\qwen.key.txt
```

新增本地 Ollama 档案：

```shell
uv run python tools/video_summary_cli.py qwen create --name "ollama-local" --api-provider ollama --base-url http://127.0.0.1:11434 --model qwen2.5vl:3b
```

设为默认模型档案：

```shell
uv run python tools/video_summary_cli.py qwen activate <qwen_profile_id>
```

### 10.3 先检索候选，再批量下载分析

推荐先只抓元数据。下面命令会按关键词搜索候选，不下载视频：

```shell
uv run python tools/video_summary_cli.py tasks start ^
  --platform bili ^
  --source-mode search ^
  --query "华南理工大学" ^
  --workflow-mode metadata_only ^
  --credential-profile-id <bili_profile_id> ^
  --headless ^
  --max-crawl-items 50 ^
  --max-videos 10 ^
  --crawl-concurrency 1 ^
  --crawl-min-sleep-seconds 5 ^
  --crawl-max-sleep-seconds 15
```

命令返回的 JSON 中会包含 `task_id`。候选视频在该任务的 `result.items` 中，每个候选都有 `id`。确认要处理的视频 ID 后，再批量下载并分析：

```shell
uv run python tools/video_summary_cli.py tasks start ^
  --platform bili ^
  --workflow-mode selected_items ^
  --source-task-id <metadata_task_id> ^
  --selected-items "117001,117002,117003" ^
  --credential-profile-id <bili_profile_id> ^
  --summarize ^
  --headless ^
  --video-upload-backend auto ^
  --crawl-concurrency 1 ^
  --analysis-concurrency 2 ^
  --crawl-min-sleep-seconds 5 ^
  --crawl-max-sleep-seconds 15
```

`--crawl-concurrency` 只控制平台检索/爬取请求；`--analysis-concurrency` 控制选中视频的本地下载、Whisper、OSS 和模型分析。保持 `1` 表示逐条处理，调到 `2` 或 `3` 可以让多个视频并行推进。

也可以重复传入多个 `--selected-item-id`：

```shell
uv run python tools/video_summary_cli.py tasks start --platform bili --workflow-mode selected_items --source-task-id <metadata_task_id> --selected-item-id 117001 --selected-item-id 117002 --summarize
```

### 10.4 一步式批量检索并分析

如果不需要人工勾选，可以用 `workflow_mode=full`。它会先检索，再对筛选后的前 `max-videos` 条进入下载和分析：

```shell
uv run python tools/video_summary_cli.py tasks start ^
  --platform bili ^
  --source-mode search ^
  --query "华南理工大学" ^
  --workflow-mode full ^
  --credential-profile-id <bili_profile_id> ^
  --summarize ^
  --headless ^
  --max-crawl-items 50 ^
  --max-videos 5 ^
  --crawl-concurrency 1 ^
  --crawl-min-sleep-seconds 5 ^
  --crawl-max-sleep-seconds 15 ^
  --crawl-long-pause-every 10 ^
  --crawl-long-pause-min-seconds 60 ^
  --crawl-long-pause-max-seconds 120
```

一步式适合明确的小批量任务；如果关键词较宽泛，仍建议使用“两段式”：先看候选，再选择视频。

### 10.5 作者与排行榜任务

作者名可能重名时，先解析候选作者：

```shell
uv run python tools/video_summary_cli.py creators resolve --platform bili --query key725
```

拿到具体 UID 后，检索该作者在日期范围内的视频：

```shell
uv run python tools/video_summary_cli.py tasks start ^
  --platform bili ^
  --source-mode creator ^
  --creator-id <creator_uid> ^
  --creator-name "key725" ^
  --workflow-mode metadata_only ^
  --credential-profile-id <bili_profile_id> ^
  --start-date YYYY-MM-DD ^
  --end-date YYYY-MM-DD ^
  --max-crawl-items 50 ^
  --max-videos 10
```

查看排行榜入口：

```shell
uv run python tools/video_summary_cli.py tasks ranking-options
uv run python tools/video_summary_cli.py tasks ranking-options --platform bili
```

抓取 B 站热门榜前 5 个候选：

```shell
uv run python tools/video_summary_cli.py tasks start ^
  --platform bili ^
  --source-mode ranking ^
  --ranking-type popular ^
  --ranking-limit 5 ^
  --workflow-mode metadata_only ^
  --credential-profile-id <bili_profile_id>
```

### 10.6 多关键词循环

PowerShell 中可以循环启动多个 metadata-only 任务：

```powershell
$keywords = @("华南理工大学", "上海交通大学计算机夏令营", "人工智能 夏令营")
foreach ($keyword in $keywords) {
  uv run python tools/video_summary_cli.py tasks start `
    --platform bili `
    --source-mode search `
    --query $keyword `
    --workflow-mode metadata_only `
    --credential-profile-id <bili_profile_id> `
    --headless `
    --max-crawl-items 40 `
    --max-videos 5 `
    --crawl-concurrency 1 `
    --crawl-min-sleep-seconds 5 `
    --crawl-max-sleep-seconds 15
}
```

每个命令都会返回独立 `task_id`。后续可以分别查看候选，再用 `selected_items` 批量处理选中的视频。

### 10.7 任务状态、停止和续跑

查询任务状态：

```shell
uv run python tools/video_summary_cli.py tasks status <task_id>
```

等待任务完成：

```shell
uv run python tools/video_summary_cli.py tasks wait <task_id>
```

停止任务：

```shell
uv run python tools/video_summary_cli.py tasks stop <task_id>
```

继续任务：

```shell
uv run python tools/video_summary_cli.py tasks resume <task_id>
```

CLI 和 WebUI 共用同一个后端任务状态。终端启动的任务也会写入：

```text
data/video_tasks/<task_id>/task_state.json
data/video_tasks/<task_id>/result.json
data/video_tasks/<task_id>/result.md
```

## 11. 常见情况

| 情况 | 处理方式 |
| --- | --- |
| 打不开 WebUI | 确认前端 5173 和后端 8080 是否都已启动。 |
| 搜不到作者 | 先尝试主页链接或平台 ID；名称重名时通过候选作者卡片确认。 |
| 封面不显示 | 平台可能没有返回封面，或封面 URL 需要登录态。 |
| 播放量显示 `-` | 平台接口没有返回该字段，界面不会填入估计值。 |
| 下载较慢 | 与平台响应、视频大小、网络和下载链接有效期有关；可减少勾选数量。 |
| Whisper 失败 | 检查 ffmpeg、PyTorch、CUDA 和 Whisper 模型是否安装。 |
| OSS 上传后占用空间 | 保持“分析结束后删除 OSS 临时视频”开启，或定期清理对象前缀。 |
| Ollama 返回 400 | 检查模型是否支持图像输入，或降低抽帧数量。 |

## 12. 文件索引

| 文件 | 说明 |
| --- | --- |
| `README.md` | 项目首页说明。 |
| `README_en.md` | 英文说明。 |
| `使用文档.md` | WebUI、CLI 和配置使用步骤。 |
| `技术报告.md` | 架构、实现方式、运行记录和耗时参考。 |
| `api/services/video_summary_manager.py` | 视频任务核心调度。 |
| `api/routers/video_summary.py` | 视频任务 API。 |
| `tools/video_summary_cli.py` | CLI 入口。 |
| `webui/src/components/video/VideoWorkspace.tsx` | 视频工作台界面。 |
| `data/video_tasks/` | 任务状态、结果、日志和默认下载目录。 |
